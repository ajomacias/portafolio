import { Element } from "react-scroll";

const Proyects = ()=>{
    return(
        <Element name="Proyects" className="min-h-screen bg-gradient-to-b from-zinc-700 to-black " >
        </Element>

    );
}

export { Proyects };